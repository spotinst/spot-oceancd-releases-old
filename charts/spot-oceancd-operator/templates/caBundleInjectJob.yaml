apiVersion: batch/v1
kind: Job
metadata:
  name: spot-oceancd-operator-cabundle-inject
  labels:
    app: spot-oceancd-operator-cabundle-inject
    {{- include "spot-oceancd-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    spec:
      serviceAccountName: spot-oceancd-operator-installer-service-account
      containers:
        - name: spot-oceancd-operator-cabundle-inject
          image: bitnami/kubectl
          imagePullPolicy: Always
          command:
            - "bin/sh"
            - "-c"
            - "caBundle=$(base64 < /run/secrets/kubernetes.io/serviceaccount/ca.crt  | tr -d '\n'); kubectl patch MutatingWebhookConfiguration controller.oceancd.spot.io -p '{\"webhooks\":[{\"name\": \"controller.oceancd.spot.io\",\"clientConfig\": {\"caBundle\": \"'${caBundle}'\"}}]}'"
      restartPolicy: Never
  backoffLimit: 1
